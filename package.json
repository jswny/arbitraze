{
	"name": "arbitraze",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "mkdir -p .wrangler/logs && WRANGLER_LOG_PATH=\"$(pwd)/.wrangler/logs\" wrangler dev --test-scheduled",
		"start": "pnpm dev",
		"cf-typegen": "wrangler types",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.42.0"
	}
}
